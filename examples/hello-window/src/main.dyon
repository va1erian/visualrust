fn main() {
    win := ui_window("VisualRust hello window", 480, 320)
    root := ui_column([
        ui_label("Hello, window!"),
        ui_label("Built from Dyon and xui")
    ])
    ui_run(win, root)
}
